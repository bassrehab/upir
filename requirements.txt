# UPIR Core Dependencies
# Clean room implementation - all dependencies are from public package repositories
#
# For development installation, use: pip install -e ".[dev]"
# For GCP integration, use: pip install -e ".[gcp]"
# For documentation, use: pip install -e ".[docs]"
# For everything, use: pip install -e ".[dev,gcp,docs]"

# Core dependencies (required)
z3-solver>=4.12.2          # SMT solving for verification
numpy>=1.24.3,<2.0.0       # Numerical computing for RL
scikit-learn>=1.3.0,<2.0.0 # Clustering for pattern extraction
